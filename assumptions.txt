1) In the case where a parameter is provided. 'Most probable' refers to whether a word starts with the parameter provided sorted in alphebatical order. Words that do not start with the parameter given are discarded

2) Additional functions may be created

3) No other imports should be made

4) the return value from the function "getNextProbableWords" must be of the type "Dict[str, List[str]]"

5) No other .py files should be created. All code should be in main.py

6) A list of strings is assumed to be polymorphic type as long as there are more than 1 word provided in the statement disregarding parameter. As only polymorphic types would refer to values inside class